# ssh-tools — Hermes SSH management plugin
name: ssh-tools
version: 0.1.0
description: "SSH terminal management — machine registry, session tracking, idle alerts, and a proper ssh_terminal tool for Hermes."
author: nexus
hooks:
  - on_session_end
